$(document).ready(function(){
    $('.slider').slick({
        slidesToShow:1,
        // speed:300,
        // autoplay:true,
        // autoplaySpeed: 2000,
        adaptiveHeight: true,
    //    dots:true
    // prevArrow: $('.prev')
    // infinite: true,
    // slidesToShow:1,
    // centerMode:true,
    // centerMode:true,
    // variableWidth:true
    });
});