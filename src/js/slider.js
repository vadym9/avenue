$(document).ready(function(){
    $('.slider').slick({
    //    dots:true
    // prevArrow: $('.prev')
    // infinite: true,
    // slidesToShow:1,
    // centerMode:true,
    // centerMode:true,
    // variableWidth:true
    });
});